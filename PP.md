# 智能体资产管理项目计划

## 1. 文档目标

本文档用于沉淀智能体资产管理系统从产品原型到首期 MVP 落地的项目计划，明确建设目标、阶段安排、交付物、协作分工、验收节点、风险与后续规划。

本计划承接：

- `PRD.md`：产品定位、功能范围、MVP 验收标准。
- `SDD.md`：页面信息架构、交互逻辑、数据模型、表结构和接口结构。
- `architecture.md`：产品架构、功能架构、技术架构和 Agent 接入边界。
- `prototype/`：1920×1080 页面原型图。
- `progress.md`：项目已完成事项、后续规划和遗留问题。

## 2. 项目目标

首期目标是建设一个面向工业动态调度智能体的资产管理 MVP，支持业务资产从创建、结构化、验证、审批、发布、Agent 使用、审计追踪、候选沉淀到价值度量的闭环。

系统需要同时满足两类使用方：

| 使用方 | 首期目标 |
| --- | --- |
| 业务用户 | 能维护规则、工作流、工具接口、案例经验、指标评估等资产，并完成验证、审批、发布和价值分析 |
| 调度智能体 | 能通过稳定接口检索资产、读取资产详情、调用授权工具、遵守治理策略、记录使用过程并提出候选资产 |

首期交付不追求一次性覆盖全部自动化能力，而是优先打通“资产可信发布 + Agent 可审计使用 + 候选资产可转正”的业务闭环。

## 3. 首期范围

### 3.1 纳入首期

| 模块 | 首期建设范围 |
| --- | --- |
| 资产工作台 | 资产概览、待处理队列、高频资产、风险资产、候选资产入口 |
| 资产台账 | 资产列表、筛选检索、详情查看、创建编辑、版本状态、批量操作 |
| 资产创建 | 分步创建、类型选择、业务描述、AI 结构化、范围权限、校验提交 |
| 候选资产池 | 候选列表、候选详情、证据链、相似资产、转正式草稿、驳回 |
| 验证与审批 | 待验证、待审批、审批记录、基础状态流转、发布控制 |
| Agent 使用记录 | 任务轨迹总览、资产引用记录、工具调用记录、治理校验记录、人工反馈记录 |
| 价值分析 | 引用次数、采纳率、覆盖任务数、治理拦截、候选转正率、低活跃资产 |
| 治理配置 | 权限配置、范围字典、审批策略、治理策略、审计规则 |
| Asset Gateway | 资产检索、资产详情、治理校验、使用记录、候选沉淀等核心接口 |
| Agent/Harness 接入 | 先通过适配层接入，保持资产核心模型与具体 Agent 框架解耦 |

### 3.2 暂缓二期

- 复杂多级审批流编排。
- 自动冲突求解和高级仿真评测。
- 跨工厂资产复制、灰度发布和分级推广。
- 完整资产推荐运营策略。
- 多 Agent 大规模并发接入和独立资产服务集群化部署。
- 与真实 MES、ERP、APS 的深度双向写回集成。

## 4. 建设原则

- 资产管理系统作为正式资产的权威来源，Agent 只能提出候选资产或草稿建议，不能直接修改正式资产。
- Agent Runtime 与资产管理模块保持逻辑独立，首期可以部署在同一后端工程中，但接口、数据模型和模块边界需要独立。
- 所有高风险动作必须经过治理校验，涉及发布、扩范围、写回、工具执行等动作时必须保留审计记录。
- 原型先完成页面结构和交互确认，再进入前端工程化实现，减少返工。
- 优先使用现有设计文档中的数据结构和接口结构，避免实现阶段重新定义核心模型。
- 遇到所需工具、库文件或命令无法执行时，需要先反馈，不自行手写替代实现。

## 5. 阶段计划

### 阶段一：原型评审与范围冻结

计划周期：2026-06-26 至 2026-07-03

| 工作项 | 说明 | 交付物 |
| --- | --- | --- |
| 原型清点 | 按模块核对 `prototype/` 中页面是否覆盖首期范围 | 原型页面清单 |
| 页面评审 | 确认主视觉、信息密度、导航结构和关键交互 | 评审意见记录 |
| MVP 范围冻结 | 明确首期必须做、可降级做、二期再做的功能 | MVP 范围确认 |
| 文档对齐 | 对齐 `PRD.md`、`SDD.md`、`architecture.md` 与原型差异 | 文档修订清单 |

阶段完成标准：

- 首期页面范围明确。
- 原型中关键页面均已评审。
- 首期功能边界、角色优先级和二期暂缓项达成一致。

### 阶段二：前端工程骨架与静态页面迁移

计划周期：2026-07-04 至 2026-07-17

| 工作项 | 说明 | 交付物 |
| --- | --- | --- |
| 前端工程初始化 | 搭建 React + TypeScript + Vite + Ant Design 工程 | `frontend/` 工程骨架 |
| 路由与布局 | 实现左侧导航、顶部历史 Tab、治理配置子菜单等通用布局 | 基础路由与布局组件 |
| Mock 数据层 | 按 `SDD.md` 页面数据结构构造 Mock 数据 | Mock 数据与类型定义 |
| 页面静态迁移 | 将原型迁移为可运行页面 | 首批可浏览页面 |
| 基础交互 | 完成筛选、页签、表格、详情抽屉、步骤表单等交互 | 可交互前端原型 |

阶段完成标准：

- 用户可以在浏览器中访问主要页面。
- 页面结构与当前原型基本一致。
- 主要列表、详情、表单和图表均可通过 Mock 数据展示。

### 阶段三：资产管理后端与核心数据闭环

计划周期：2026-07-18 至 2026-08-07

| 工作项 | 说明 | 交付物 |
| --- | --- | --- |
| 后端工程初始化 | 搭建 FastAPI 或最终确认的后端工程结构 | 后端基础工程 |
| 数据库模型 | 按 `SDD.md` 建立资产、版本、范围、权限、候选、审计等核心表 | 数据库迁移脚本 |
| 资产台账接口 | 实现资产列表、详情、创建、编辑、状态流转接口 | 资产 API |
| 候选资产接口 | 实现候选列表、详情、转草稿、驳回、证据查询接口 | 候选资产 API |
| 验证审批接口 | 实现验证任务、审批实例、审批步骤和发布控制 | 验证审批 API |
| 前后端联调 | 前端从 Mock 数据切换到接口数据 | 联调版本 |

阶段完成标准：

- 可以创建资产草稿并进入验证审批流程。
- 可以从候选资产转为正式资产草稿。
- 资产发布前必须经过验证和审批状态流转。
- 前端核心页面可以使用真实接口数据运行。

### 阶段四：Agent 使用记录与 Asset Gateway

计划周期：2026-08-08 至 2026-08-21

| 工作项 | 说明 | 交付物 |
| --- | --- | --- |
| Asset Gateway 接口 | 实现 `search_assets`、`get_asset`、`check_policy`、`record_usage`、`propose_candidate` | 网关接口 |
| Agent 任务轨迹 | 记录任务上下文、任务节点、建议结果和人工反馈 | 任务轨迹 API |
| 资产引用记录 | 记录检索、引用、丢弃原因和方案影响 | 资产引用 API |
| 工具调用记录 | 记录调用入参、出参摘要、状态、耗时、错误和审批关联 | 工具调用 API |
| 治理校验记录 | 记录策略命中、风险等级、下一步动作和审批要求 | 治理审计 API |
| 使用记录页面联调 | 联调任务轨迹、资产引用、工具调用、治理校验、人工反馈页面 | Agent 使用记录联调版 |

阶段完成标准：

- Agent 或模拟 Agent 可以通过 Asset Gateway 使用资产能力。
- 每次资产检索、引用、工具调用和治理校验都能形成审计记录。
- 业务人员可以从任务视角复盘 Agent 为什么引用资产、如何影响结果。

### 阶段五：价值分析、治理配置与验收加固

计划周期：2026-08-22 至 2026-09-04

| 工作项 | 说明 | 交付物 |
| --- | --- | --- |
| 价值指标汇总 | 实现引用次数、采纳率、候选转正率、低活跃资产等统计 | 指标汇总任务 |
| 价值分析页面 | 联调趋势、排行、贡献和待优化资产 | 价值分析联调版 |
| 治理配置页面 | 联调权限配置、范围字典、审批策略、治理策略、审计规则 | 治理配置联调版 |
| 权限与风险校验 | 校验 Agent 读取、调用、建议、申请和执行权限边界 | 权限校验结果 |
| 验收测试 | 按 PRD 验收标准完成业务场景测试 | 验收报告 |
| 缺陷修复 | 修复阻断缺陷和高优先级体验问题 | MVP 候选版本 |

阶段完成标准：

- 首期验收标准全部有可演示路径。
- 核心业务闭环可以完整跑通。
- 高风险权限、审批和审计链路没有阻断缺陷。

## 6. 关键里程碑

| 里程碑 | 计划时间 | 判断标准 |
| --- | --- | --- |
| M1 原型与范围冻结 | 2026-07-03 | 首期页面、功能边界、角色优先级确认完成 |
| M2 前端可交互原型 | 2026-07-17 | 主要页面可运行，支持 Mock 数据交互 |
| M3 资产管理闭环联调 | 2026-08-07 | 资产创建、验证、审批、发布、候选转草稿跑通 |
| M4 Agent 使用审计联调 | 2026-08-21 | Asset Gateway 与使用记录链路跑通 |
| M5 MVP 验收候选版 | 2026-09-04 | 价值分析、治理配置、验收测试和缺陷修复完成 |

## 7. 交付物清单

| 类别 | 交付物 |
| --- | --- |
| 产品文档 | `PRD.md`、`PP.md`、评审记录、验收报告 |
| 设计文档 | `SDD.md`、页面字段映射、接口字段映射 |
| 架构文档 | `architecture.md`、模块边界说明、部署说明 |
| 原型资产 | `prototype/` 下的 1920×1080 页面原型图 |
| 前端工程 | React + TypeScript + Vite + Ant Design 页面工程 |
| 后端工程 | 资产管理 API、Asset Gateway、验证审批、审计记录和价值统计 |
| 数据库 | 表结构迁移、枚举字典、初始化数据和测试数据 |
| 测试资产 | 功能测试用例、接口测试用例、验收场景数据 |

## 8. 优先级与降级策略

| 优先级 | 内容 | 降级策略 |
| --- | --- | --- |
| P0 | 资产创建、资产台账、资产详情、验证审批、发布控制 | 不降级，必须形成主闭环 |
| P0 | Agent 使用记录、治理校验记录、候选资产转草稿 | 可先用模拟 Agent 数据，但审计链路必须存在 |
| P1 | 价值分析、治理配置、范围权限配置 | 首期可先覆盖基础指标和基础配置 |
| P1 | AI 辅助结构化、相似资产识别、治理预检查 | 可先通过 Harness Adapter 预留接口，首期以半自动方式演示 |
| P2 | 高级图谱、复杂审批编排、跨工厂复制、仿真评测 | 延后到二期 |

## 9. 协作分工建议

| 角色 | 主要职责 |
| --- | --- |
| 产品负责人 | 确认业务范围、角色优先级、验收标准和原型评审意见 |
| 交互/视觉设计 | 维护原型一致性、页面信息密度和视觉规范 |
| 前端开发 | 实现页面、路由、组件、状态管理、接口联调和图表展示 |
| 后端开发 | 实现资产模型、状态流转、验证审批、审计记录、价值统计和接口服务 |
| Agent/Harness 开发 | 实现 Agent Asset Adapter、Harness Adapter、候选生成和资产使用记录上报 |
| 测试/验收 | 维护测试用例、验收场景、缺陷跟踪和发布质量门禁 |
| 业务专家 | 确认资产类型字段、审批口径、风险边界、样例任务和价值指标口径 |

## 10. 测试与验收计划

### 10.1 核心测试场景

| 场景 | 验证重点 |
| --- | --- |
| 人工创建规则资产 | 类型字段、适用范围、风险边界、验证审批、发布状态 |
| 编辑已发布资产 | 是否生成新版本草稿，是否保留历史版本 |
| 候选资产转正 | 候选证据、相似资产、转草稿、验证审批链路 |
| Agent 引用资产 | 检索记录、引用原因、引用影响、人工反馈 |
| 工具调用治理 | 权限校验、策略命中、审批要求、调用审计 |
| 价值分析 | 指标汇总、趋势图、排行、低活跃资产识别 |
| 权限配置 | 主体权限、适用范围、工具动作边界和风险预览 |

### 10.2 首期验收标准

- 用户可以创建至少 5 类核心资产，并按资产类型展示不同字段。
- 已发布资产必须经过验证和审批状态流转。
- 智能体任务可以记录资产检索、引用、治理校验、工具调用和人工反馈。
- 智能体可以提交候选资产，但不能直接修改正式资产。
- 用户可以从候选资产转为正式资产草稿并继续验证审批。
- 用户可以查看资产引用次数、采纳率、候选转正率等基础价值指标。
- 每项资产都能追踪来源、版本、审批、使用记录和证据链。

## 11. 主要风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 首期范围过大 | 影响 MVP 交付节奏 | 坚持 P0 闭环优先，P1 能演示即可，P2 延后 |
| 角色和审批口径未确认 | 影响权限、审批和页面字段设计 | 阶段一完成角色优先级与审批口径确认 |
| Agent/Harness 选型变动 | 影响辅助创建和候选沉淀实现 | 通过 Harness Adapter 隔离具体框架 |
| 真实业务系统接口暂不可用 | 影响工具资产和调用演示 | 首期可使用模拟接口，但保留真实接口协议边界 |
| 数据模型复杂度较高 | 影响后端开发和联调 | 优先实现核心表和核心状态流转，扩展表按资产类型逐步补齐 |
| 价值指标口径不稳定 | 影响价值分析可信度 | 首期先沉淀基础指标，复杂经营指标后续迭代 |

## 12. 当前待确认事项

- 首期角色优先级：计划员、计划主管、工艺专家、IT 管理员、生产经理是否全部覆盖。
- 首期是否接入真实 MES、ERP、APS，还是先采用模拟接口和测试数据。
- 高风险工具是否允许 Agent 发起审批，还是仅允许生成建议。
- 资产审批是否接入企业现有 OA/BPM，还是首期内置轻量审批。
- 首期 Harness 接入优先选择 Hermes、DeerFlow、DeepAgents/LangGraph 还是自研适配。
- 首期后端是否统一采用 Python FastAPI，还是资产管理核心服务直接采用 Java/Kotlin。

## 13. 后续维护方式

- 当产品范围、里程碑或交付计划发生变化时，优先更新本文档。
- 当已有事项完成时，在 `progress.md` 的“已完成”中追加带日期的完成记录。
- 当发现新风险、新依赖或新待确认问题时，在本文档对应章节补充，并同步到 `progress.md` 的遗留问题或后续规划中。
